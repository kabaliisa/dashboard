import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FraudpreventedComponent } from './fraudprevented.component';

describe('FraudpreventedComponent', () => {
  let component: FraudpreventedComponent;
  let fixture: ComponentFixture<FraudpreventedComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ FraudpreventedComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(FraudpreventedComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
